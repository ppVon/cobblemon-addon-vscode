# Fossils Schema

Schema: `schema.json`

Use for files in `data/cobblemon/fossils/*.json`.

Key fields:
- `result`: Pokemon properties produced by revival.
- `fossils`: required fossil item predicates/conditions.

Common pitfalls:
- Empty `fossils` list.
- Invalid `result` format.
