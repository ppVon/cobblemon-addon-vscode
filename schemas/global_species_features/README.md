# Global Species Features Schema

Schema: `schema.json`

Use for files in `data/cobblemon/global_species_features/*.json`.

This format is identical to `species_features`; it defines feature providers applied globally.
