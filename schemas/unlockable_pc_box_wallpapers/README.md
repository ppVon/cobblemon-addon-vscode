# Unlockable Wallpapers Schema

Schema: `schema.json`

Use for files in `data/cobblemon/unlockable_pc_box_wallpapers/*.json`.

Key fields:
- `enabled`: toggle availability.
- `displayName`: optional translation key/text.
- `texture`: texture id.
