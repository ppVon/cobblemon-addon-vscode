# Starters Schema

Schema: `schema.json`

Use for files in `data/cobblemon/starters/*.json`.

Key fields:
- `name`: starter category id/name.
- `displayName`: optional display label.
- `pokemon`: non-empty starter list.
