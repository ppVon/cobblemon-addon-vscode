# Dex Entry Additions Schema

Schema: `schema.json`

Use for files in `data/cobblemon/dex_entry_additions/*.json`.

Key fields:
- `entryId`: target dex entry id.
- `forms`: extra display/unlock forms.
- `variations`: extra cosmetic variations.
