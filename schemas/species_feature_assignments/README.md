# Species Feature Assignments Schema

Schema: `schema.json`

Use for files in `data/cobblemon/species_feature_assignments/*.json`.

Key fields:
- `pokemon`: selectors for affected species/forms.
- `features`: feature keys to apply.
