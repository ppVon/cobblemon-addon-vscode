# Dex Additions Schema

Schema: `schema.json`

Use for files in `data/cobblemon/dex_additions/*.json`.

Key fields:
- `dexId`: target dex id.
- `entries`: entries or sub-dex ids appended to that dex.
